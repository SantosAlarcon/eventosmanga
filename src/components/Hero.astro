---

---

<main class="hero__container">
    <section class="hero__subbg">
        <section class="hero__inner">
            <section class="hero__flair">
                🎌 ¡Arranca la temporada de eventos de 2026!
            </section>
            <section class="hero__title">
                La aventura <span class="hero__title__span">otaku</span><br />
                te espera
            </section>
            <section class="hero__description">
                <p>
                    En <b>Eventos Manga</b> encontrarás el calendario más completo
                    de
                    <b>salones</b>, <b>ferias</b>, <b>concursos</b> y demás actividades
                    relacionadas con el
                    <b>manga y anime</b> en <b>España</b>.
                </p>
                <p class="hero__cta">
                    ¡Manténte informado y únete a la comunidad otaku!
                </p>
            </section>
            <section class="hero__buttons">
                <a
                    class="hero__button"
                    href="/eventos"
                    aria-label="Ir a la lista de eventos"
                    >Descubrir nuevos eventos <span
                        class="hero__button__left-arrow">&rarr;</span
                    ></a
                >
            </section>
        </section>
    </section>
</main>
<style>
    .hero__container {
        background-image: url("/Hero.webp");
        background-size: cover;
        max-width: 100%;
        padding: 0;
    }

    .hero__subbg {
        background: linear-gradient(45deg, #119f 0%, #f608 100%);
        background-blend-mode: darken;
        width: 100%;
        margin: 0;
    }

    .hero__inner {
        padding: 4rem 3rem;
        display: flex;
        flex-direction: column;

        @media (width <=768px) {
            align-items: center;
            text-align: center;
        }
    }

    .hero__flair {
        text-transform: uppercase;
        font-weight: 600;
        font-size: 0.8rem;
        border-radius: 2rem;
        border: 1px #fff6 solid;
        max-width: fit-content;
        padding: 0.75rem 1rem;
        backdrop-filter: blur(0.5rem);
        background-color: #fff2;
    }

    .hero__title {
        text-transform: uppercase;
        padding: 1.5rem 0;
        font-size: 5.5rem;
        font-weight: 900;
        line-height: 6rem;
        width: 50%;

        @media (max-width: 768px) {
            font-size: 4rem;
            line-height: 5rem;
            width: 100%;
        }
    }

    .hero__title__span {
        color: #f6f;
    }

    .hero__description {
        width: 50%;
        line-height: 1.75rem;

        @media (max-width: 768px) {
            width: 100%;
            text-align: center;
        }
    }

    .hero__cta {
        font-size: 2rem;
        font-weight: 900;
        padding: 1rem 0;
        line-height: 2.5rem;
    }

    .hero__buttons {
        display: flex;
        margin-top: 2rem;

        @media (max-width: 768px) {
            flex-direction: column;
            align-items: center;
            margin-top: 1rem;
        }
    }

    .hero__button {
        background-color: #a338;
        backdrop-filter: blur(.25rem);
        text-decoration: none;
        color: #eee;
        padding: 1rem 2.5rem 1rem 2rem;
        border-radius: 3rem;
        font-weight: 700;
        font-size: clamp(1.1rem, 2vw, 1.25rem);
        transition: 0.2s ease-in-out;
        border: 1px #eee6 solid;

        &:hover {
            background-color: #e228;

            & .hero__button__left-arrow {
                transform: translateX(0.5rem);
            }
        }
    }

    .hero__button__left-arrow {
        padding-left: 0.5rem;
        display: inline-block;
        transition: .2s ease-in-out;
    }
</style>
